import React from 'react'
import PropTypes from 'prop-types'

function PropertyCard({ code }) {
  return <div></div>
}

PropertyCard.propTypes = {
  code: PropTypes.string.isRequired,
}

export default PropertyCard
